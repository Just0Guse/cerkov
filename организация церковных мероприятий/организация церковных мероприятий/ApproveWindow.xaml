<Window x:Class="организация_церковных_мероприятий.ApproveWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:организация_церковных_мероприятий"
        mc:Ignorable="d"
        Title="ApproveWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Заявки на согласование от операторов" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <DataGrid x:Name="UnapprovedGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Title}"/>
                <DataGridTextColumn Header="Место" Binding="{Binding Location}"/>
                <DataGridTextColumn Header="Время" Binding="{Binding DateTime}"/>
                <DataGridTextColumn Header="Организатор" Binding="{Binding Organizer}"/>
                <DataGridTextColumn Header="Контакты" Binding="{Binding Contacts}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Одобрить выбранное" Click="ApproveClick" Width="150" Height="30" Margin="5"/>
            <Button Content="Отклонить выбранное" Click="RejectClick" Width="150" Height="30" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
